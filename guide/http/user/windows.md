# Windows

## GET /user/windows

Auth: Key or Bearer

Response: `Vec<Window>` — array. Window: hex, tenant, user, name, timezone, priority, start?, end?, busytype, rrule?, slots, active, meta, created, updated