# Scheduling (`hermes.scheduling`)

Booking services, appointments, and availability.

```ts
const services = await hermes.scheduling.services();
const slots = await hermes.scheduling.availability(start, end);
```

API reference: [guide/http/booking.md](../../../guide/http/booking.md), [user/appointments.md](../../../guide/http/user/appointments.md).
