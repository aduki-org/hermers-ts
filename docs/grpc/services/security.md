# Security (`client.security`)

Wraps `hermes.security.SecurityService`.

```ts
const status = await client.security.status();
```

API reference: [guide/grpc/security.md](../../../guide/grpc/security.md).
