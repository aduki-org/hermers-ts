# Storage (`client.storage`)

Wraps `hermes.storage.StorageService`.

```ts
await client.storage.put({ key: 'file.txt', data: new Uint8Array([1, 2, 3]) });
```

API reference: [guide/grpc/storage.md](../../../guide/grpc/storage.md).
