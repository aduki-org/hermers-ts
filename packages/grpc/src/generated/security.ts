// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: security.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export interface StatusReq {
}

export interface MtastsEntry {
  domain: string;
  policyJson: string;
  expires: string;
}

export interface TlsaEntry {
  host: string;
  port: number;
  recordsJson: string;
  expires: string;
}

export interface BimiEntry {
  domain: string;
  location?: string | undefined;
  vmc?: string | undefined;
  expires: string;
}

export interface ReportEntry {
  hex: string;
  kind: string;
  domain: string;
  period: string;
  received: string;
}

export interface StatusResp {
  mtasts: MtastsEntry[];
  tlsa: TlsaEntry[];
  bimi: BimiEntry[];
  reports: ReportEntry[];
}

function createBaseStatusReq(): StatusReq {
  return {};
}

export const StatusReq: MessageFns<StatusReq> = {
  encode(_: StatusReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): StatusReq {
    return {};
  },

  toJSON(_: StatusReq): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<StatusReq>, I>>(base?: I): StatusReq {
    return StatusReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StatusReq>, I>>(_: I): StatusReq {
    const message = createBaseStatusReq();
    return message;
  },
};

function createBaseMtastsEntry(): MtastsEntry {
  return { domain: "", policyJson: "", expires: "" };
}

export const MtastsEntry: MessageFns<MtastsEntry> = {
  encode(message: MtastsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.policyJson !== "") {
      writer.uint32(18).string(message.policyJson);
    }
    if (message.expires !== "") {
      writer.uint32(26).string(message.expires);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MtastsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMtastsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.policyJson = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.expires = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MtastsEntry {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      policyJson: isSet(object.policyJson)
        ? globalThis.String(object.policyJson)
        : isSet(object.policy_json)
        ? globalThis.String(object.policy_json)
        : "",
      expires: isSet(object.expires) ? globalThis.String(object.expires) : "",
    };
  },

  toJSON(message: MtastsEntry): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.policyJson !== "") {
      obj.policyJson = message.policyJson;
    }
    if (message.expires !== "") {
      obj.expires = message.expires;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MtastsEntry>, I>>(base?: I): MtastsEntry {
    return MtastsEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MtastsEntry>, I>>(object: I): MtastsEntry {
    const message = createBaseMtastsEntry();
    message.domain = object.domain ?? "";
    message.policyJson = object.policyJson ?? "";
    message.expires = object.expires ?? "";
    return message;
  },
};

function createBaseTlsaEntry(): TlsaEntry {
  return { host: "", port: 0, recordsJson: "", expires: "" };
}

export const TlsaEntry: MessageFns<TlsaEntry> = {
  encode(message: TlsaEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.host !== "") {
      writer.uint32(10).string(message.host);
    }
    if (message.port !== 0) {
      writer.uint32(16).int32(message.port);
    }
    if (message.recordsJson !== "") {
      writer.uint32(26).string(message.recordsJson);
    }
    if (message.expires !== "") {
      writer.uint32(34).string(message.expires);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TlsaEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTlsaEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.host = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.port = reader.int32();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.recordsJson = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expires = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TlsaEntry {
    return {
      host: isSet(object.host) ? globalThis.String(object.host) : "",
      port: isSet(object.port) ? globalThis.Number(object.port) : 0,
      recordsJson: isSet(object.recordsJson)
        ? globalThis.String(object.recordsJson)
        : isSet(object.records_json)
        ? globalThis.String(object.records_json)
        : "",
      expires: isSet(object.expires) ? globalThis.String(object.expires) : "",
    };
  },

  toJSON(message: TlsaEntry): unknown {
    const obj: any = {};
    if (message.host !== "") {
      obj.host = message.host;
    }
    if (message.port !== 0) {
      obj.port = Math.round(message.port);
    }
    if (message.recordsJson !== "") {
      obj.recordsJson = message.recordsJson;
    }
    if (message.expires !== "") {
      obj.expires = message.expires;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<TlsaEntry>, I>>(base?: I): TlsaEntry {
    return TlsaEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TlsaEntry>, I>>(object: I): TlsaEntry {
    const message = createBaseTlsaEntry();
    message.host = object.host ?? "";
    message.port = object.port ?? 0;
    message.recordsJson = object.recordsJson ?? "";
    message.expires = object.expires ?? "";
    return message;
  },
};

function createBaseBimiEntry(): BimiEntry {
  return { domain: "", location: undefined, vmc: undefined, expires: "" };
}

export const BimiEntry: MessageFns<BimiEntry> = {
  encode(message: BimiEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.location !== undefined) {
      writer.uint32(18).string(message.location);
    }
    if (message.vmc !== undefined) {
      writer.uint32(26).string(message.vmc);
    }
    if (message.expires !== "") {
      writer.uint32(34).string(message.expires);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BimiEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBimiEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.location = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.vmc = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.expires = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BimiEntry {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      location: isSet(object.location) ? globalThis.String(object.location) : undefined,
      vmc: isSet(object.vmc) ? globalThis.String(object.vmc) : undefined,
      expires: isSet(object.expires) ? globalThis.String(object.expires) : "",
    };
  },

  toJSON(message: BimiEntry): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.location !== undefined) {
      obj.location = message.location;
    }
    if (message.vmc !== undefined) {
      obj.vmc = message.vmc;
    }
    if (message.expires !== "") {
      obj.expires = message.expires;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BimiEntry>, I>>(base?: I): BimiEntry {
    return BimiEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BimiEntry>, I>>(object: I): BimiEntry {
    const message = createBaseBimiEntry();
    message.domain = object.domain ?? "";
    message.location = object.location ?? undefined;
    message.vmc = object.vmc ?? undefined;
    message.expires = object.expires ?? "";
    return message;
  },
};

function createBaseReportEntry(): ReportEntry {
  return { hex: "", kind: "", domain: "", period: "", received: "" };
}

export const ReportEntry: MessageFns<ReportEntry> = {
  encode(message: ReportEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hex !== "") {
      writer.uint32(10).string(message.hex);
    }
    if (message.kind !== "") {
      writer.uint32(18).string(message.kind);
    }
    if (message.domain !== "") {
      writer.uint32(26).string(message.domain);
    }
    if (message.period !== "") {
      writer.uint32(34).string(message.period);
    }
    if (message.received !== "") {
      writer.uint32(42).string(message.received);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReportEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReportEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hex = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.domain = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.period = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.received = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReportEntry {
    return {
      hex: isSet(object.hex) ? globalThis.String(object.hex) : "",
      kind: isSet(object.kind) ? globalThis.String(object.kind) : "",
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      period: isSet(object.period) ? globalThis.String(object.period) : "",
      received: isSet(object.received) ? globalThis.String(object.received) : "",
    };
  },

  toJSON(message: ReportEntry): unknown {
    const obj: any = {};
    if (message.hex !== "") {
      obj.hex = message.hex;
    }
    if (message.kind !== "") {
      obj.kind = message.kind;
    }
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.period !== "") {
      obj.period = message.period;
    }
    if (message.received !== "") {
      obj.received = message.received;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReportEntry>, I>>(base?: I): ReportEntry {
    return ReportEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReportEntry>, I>>(object: I): ReportEntry {
    const message = createBaseReportEntry();
    message.hex = object.hex ?? "";
    message.kind = object.kind ?? "";
    message.domain = object.domain ?? "";
    message.period = object.period ?? "";
    message.received = object.received ?? "";
    return message;
  },
};

function createBaseStatusResp(): StatusResp {
  return { mtasts: [], tlsa: [], bimi: [], reports: [] };
}

export const StatusResp: MessageFns<StatusResp> = {
  encode(message: StatusResp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.mtasts) {
      MtastsEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.tlsa) {
      TlsaEntry.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.bimi) {
      BimiEntry.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.reports) {
      ReportEntry.encode(v!, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StatusResp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatusResp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mtasts.push(MtastsEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.tlsa.push(TlsaEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.bimi.push(BimiEntry.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.reports.push(ReportEntry.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StatusResp {
    return {
      mtasts: globalThis.Array.isArray(object?.mtasts) ? object.mtasts.map((e: any) => MtastsEntry.fromJSON(e)) : [],
      tlsa: globalThis.Array.isArray(object?.tlsa) ? object.tlsa.map((e: any) => TlsaEntry.fromJSON(e)) : [],
      bimi: globalThis.Array.isArray(object?.bimi) ? object.bimi.map((e: any) => BimiEntry.fromJSON(e)) : [],
      reports: globalThis.Array.isArray(object?.reports) ? object.reports.map((e: any) => ReportEntry.fromJSON(e)) : [],
    };
  },

  toJSON(message: StatusResp): unknown {
    const obj: any = {};
    if (message.mtasts?.length) {
      obj.mtasts = message.mtasts.map((e) => MtastsEntry.toJSON(e));
    }
    if (message.tlsa?.length) {
      obj.tlsa = message.tlsa.map((e) => TlsaEntry.toJSON(e));
    }
    if (message.bimi?.length) {
      obj.bimi = message.bimi.map((e) => BimiEntry.toJSON(e));
    }
    if (message.reports?.length) {
      obj.reports = message.reports.map((e) => ReportEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StatusResp>, I>>(base?: I): StatusResp {
    return StatusResp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StatusResp>, I>>(object: I): StatusResp {
    const message = createBaseStatusResp();
    message.mtasts = object.mtasts?.map((e) => MtastsEntry.fromPartial(e)) || [];
    message.tlsa = object.tlsa?.map((e) => TlsaEntry.fromPartial(e)) || [];
    message.bimi = object.bimi?.map((e) => BimiEntry.fromPartial(e)) || [];
    message.reports = object.reports?.map((e) => ReportEntry.fromPartial(e)) || [];
    return message;
  },
};

export type SecurityServiceService = typeof SecurityServiceService;
export const SecurityServiceService = {
  status: {
    path: "/hermes.security.SecurityService/Status" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: StatusReq): Buffer => Buffer.from(StatusReq.encode(value).finish()),
    requestDeserialize: (value: Buffer): StatusReq => StatusReq.decode(value),
    responseSerialize: (value: StatusResp): Buffer => Buffer.from(StatusResp.encode(value).finish()),
    responseDeserialize: (value: Buffer): StatusResp => StatusResp.decode(value),
  },
} as const;

export interface SecurityServiceServer extends UntypedServiceImplementation {
  status: handleUnaryCall<StatusReq, StatusResp>;
}

export interface SecurityServiceClient extends Client {
  status(request: StatusReq, callback: (error: ServiceError | null, response: StatusResp) => void): ClientUnaryCall;
  status(
    request: StatusReq,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: StatusResp) => void,
  ): ClientUnaryCall;
  status(
    request: StatusReq,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: StatusResp) => void,
  ): ClientUnaryCall;
}

export const SecurityServiceClient = makeGenericClientConstructor(
  SecurityServiceService,
  "hermes.security.SecurityService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): SecurityServiceClient;
  service: typeof SecurityServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
