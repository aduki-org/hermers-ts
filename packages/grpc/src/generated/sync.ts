// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v3.21.12
// source: sync.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";
import { Timestamp } from "./google/protobuf/timestamp.js";

export interface ContactSyncReq {
  tenant: string;
  since?: Date | undefined;
}

export interface ContactSyncResp {
  changed: string[];
  removed: string[];
  ctag: string;
}

export interface MailboxSyncReq {
  mailbox: string;
  knownUidvalidity: number;
  knownModseq: number;
}

export interface MailboxSyncResp {
  newUids: number[];
  changedUids: number[];
  removedUids: number[];
  modseq: number;
  uidvalidity: number;
}

function createBaseContactSyncReq(): ContactSyncReq {
  return { tenant: "", since: undefined };
}

export const ContactSyncReq: MessageFns<ContactSyncReq> = {
  encode(message: ContactSyncReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tenant !== "") {
      writer.uint32(10).string(message.tenant);
    }
    if (message.since !== undefined) {
      Timestamp.encode(toTimestamp(message.since), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContactSyncReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContactSyncReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.tenant = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.since = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContactSyncReq {
    return {
      tenant: isSet(object.tenant) ? globalThis.String(object.tenant) : "",
      since: isSet(object.since) ? fromJsonTimestamp(object.since) : undefined,
    };
  },

  toJSON(message: ContactSyncReq): unknown {
    const obj: any = {};
    if (message.tenant !== "") {
      obj.tenant = message.tenant;
    }
    if (message.since !== undefined) {
      obj.since = message.since.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContactSyncReq>, I>>(base?: I): ContactSyncReq {
    return ContactSyncReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContactSyncReq>, I>>(object: I): ContactSyncReq {
    const message = createBaseContactSyncReq();
    message.tenant = object.tenant ?? "";
    message.since = object.since ?? undefined;
    return message;
  },
};

function createBaseContactSyncResp(): ContactSyncResp {
  return { changed: [], removed: [], ctag: "" };
}

export const ContactSyncResp: MessageFns<ContactSyncResp> = {
  encode(message: ContactSyncResp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.changed) {
      writer.uint32(10).string(v!);
    }
    for (const v of message.removed) {
      writer.uint32(18).string(v!);
    }
    if (message.ctag !== "") {
      writer.uint32(26).string(message.ctag);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContactSyncResp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContactSyncResp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.changed.push(reader.string());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.removed.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.ctag = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContactSyncResp {
    return {
      changed: globalThis.Array.isArray(object?.changed) ? object.changed.map((e: any) => globalThis.String(e)) : [],
      removed: globalThis.Array.isArray(object?.removed) ? object.removed.map((e: any) => globalThis.String(e)) : [],
      ctag: isSet(object.ctag) ? globalThis.String(object.ctag) : "",
    };
  },

  toJSON(message: ContactSyncResp): unknown {
    const obj: any = {};
    if (message.changed?.length) {
      obj.changed = message.changed;
    }
    if (message.removed?.length) {
      obj.removed = message.removed;
    }
    if (message.ctag !== "") {
      obj.ctag = message.ctag;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ContactSyncResp>, I>>(base?: I): ContactSyncResp {
    return ContactSyncResp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContactSyncResp>, I>>(object: I): ContactSyncResp {
    const message = createBaseContactSyncResp();
    message.changed = object.changed?.map((e) => e) || [];
    message.removed = object.removed?.map((e) => e) || [];
    message.ctag = object.ctag ?? "";
    return message;
  },
};

function createBaseMailboxSyncReq(): MailboxSyncReq {
  return { mailbox: "", knownUidvalidity: 0, knownModseq: 0 };
}

export const MailboxSyncReq: MessageFns<MailboxSyncReq> = {
  encode(message: MailboxSyncReq, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mailbox !== "") {
      writer.uint32(10).string(message.mailbox);
    }
    if (message.knownUidvalidity !== 0) {
      writer.uint32(16).uint32(message.knownUidvalidity);
    }
    if (message.knownModseq !== 0) {
      writer.uint32(24).uint64(message.knownModseq);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MailboxSyncReq {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMailboxSyncReq();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.mailbox = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.knownUidvalidity = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.knownModseq = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MailboxSyncReq {
    return {
      mailbox: isSet(object.mailbox) ? globalThis.String(object.mailbox) : "",
      knownUidvalidity: isSet(object.knownUidvalidity)
        ? globalThis.Number(object.knownUidvalidity)
        : isSet(object.known_uidvalidity)
        ? globalThis.Number(object.known_uidvalidity)
        : 0,
      knownModseq: isSet(object.knownModseq)
        ? globalThis.Number(object.knownModseq)
        : isSet(object.known_modseq)
        ? globalThis.Number(object.known_modseq)
        : 0,
    };
  },

  toJSON(message: MailboxSyncReq): unknown {
    const obj: any = {};
    if (message.mailbox !== "") {
      obj.mailbox = message.mailbox;
    }
    if (message.knownUidvalidity !== 0) {
      obj.knownUidvalidity = Math.round(message.knownUidvalidity);
    }
    if (message.knownModseq !== 0) {
      obj.knownModseq = Math.round(message.knownModseq);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MailboxSyncReq>, I>>(base?: I): MailboxSyncReq {
    return MailboxSyncReq.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MailboxSyncReq>, I>>(object: I): MailboxSyncReq {
    const message = createBaseMailboxSyncReq();
    message.mailbox = object.mailbox ?? "";
    message.knownUidvalidity = object.knownUidvalidity ?? 0;
    message.knownModseq = object.knownModseq ?? 0;
    return message;
  },
};

function createBaseMailboxSyncResp(): MailboxSyncResp {
  return { newUids: [], changedUids: [], removedUids: [], modseq: 0, uidvalidity: 0 };
}

export const MailboxSyncResp: MessageFns<MailboxSyncResp> = {
  encode(message: MailboxSyncResp, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.newUids) {
      writer.uint32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.changedUids) {
      writer.uint32(v);
    }
    writer.join();
    writer.uint32(26).fork();
    for (const v of message.removedUids) {
      writer.uint32(v);
    }
    writer.join();
    if (message.modseq !== 0) {
      writer.uint32(32).uint64(message.modseq);
    }
    if (message.uidvalidity !== 0) {
      writer.uint32(40).uint32(message.uidvalidity);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MailboxSyncResp {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMailboxSyncResp();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.newUids.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.newUids.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.changedUids.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.changedUids.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag === 24) {
            message.removedUids.push(reader.uint32());

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.removedUids.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.modseq = longToNumber(reader.uint64());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.uidvalidity = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MailboxSyncResp {
    return {
      newUids: globalThis.Array.isArray(object?.newUids)
        ? object.newUids.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.new_uids)
        ? object.new_uids.map((e: any) => globalThis.Number(e))
        : [],
      changedUids: globalThis.Array.isArray(object?.changedUids)
        ? object.changedUids.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.changed_uids)
        ? object.changed_uids.map((e: any) => globalThis.Number(e))
        : [],
      removedUids: globalThis.Array.isArray(object?.removedUids)
        ? object.removedUids.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.removed_uids)
        ? object.removed_uids.map((e: any) => globalThis.Number(e))
        : [],
      modseq: isSet(object.modseq) ? globalThis.Number(object.modseq) : 0,
      uidvalidity: isSet(object.uidvalidity) ? globalThis.Number(object.uidvalidity) : 0,
    };
  },

  toJSON(message: MailboxSyncResp): unknown {
    const obj: any = {};
    if (message.newUids?.length) {
      obj.newUids = message.newUids.map((e) => Math.round(e));
    }
    if (message.changedUids?.length) {
      obj.changedUids = message.changedUids.map((e) => Math.round(e));
    }
    if (message.removedUids?.length) {
      obj.removedUids = message.removedUids.map((e) => Math.round(e));
    }
    if (message.modseq !== 0) {
      obj.modseq = Math.round(message.modseq);
    }
    if (message.uidvalidity !== 0) {
      obj.uidvalidity = Math.round(message.uidvalidity);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MailboxSyncResp>, I>>(base?: I): MailboxSyncResp {
    return MailboxSyncResp.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MailboxSyncResp>, I>>(object: I): MailboxSyncResp {
    const message = createBaseMailboxSyncResp();
    message.newUids = object.newUids?.map((e) => e) || [];
    message.changedUids = object.changedUids?.map((e) => e) || [];
    message.removedUids = object.removedUids?.map((e) => e) || [];
    message.modseq = object.modseq ?? 0;
    message.uidvalidity = object.uidvalidity ?? 0;
    return message;
  },
};

export type SyncServiceService = typeof SyncServiceService;
export const SyncServiceService = {
  contacts: {
    path: "/hermes.sync.SyncService/Contacts" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: ContactSyncReq): Buffer => Buffer.from(ContactSyncReq.encode(value).finish()),
    requestDeserialize: (value: Buffer): ContactSyncReq => ContactSyncReq.decode(value),
    responseSerialize: (value: ContactSyncResp): Buffer => Buffer.from(ContactSyncResp.encode(value).finish()),
    responseDeserialize: (value: Buffer): ContactSyncResp => ContactSyncResp.decode(value),
  },
  mailboxes: {
    path: "/hermes.sync.SyncService/Mailboxes" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: MailboxSyncReq): Buffer => Buffer.from(MailboxSyncReq.encode(value).finish()),
    requestDeserialize: (value: Buffer): MailboxSyncReq => MailboxSyncReq.decode(value),
    responseSerialize: (value: MailboxSyncResp): Buffer => Buffer.from(MailboxSyncResp.encode(value).finish()),
    responseDeserialize: (value: Buffer): MailboxSyncResp => MailboxSyncResp.decode(value),
  },
} as const;

export interface SyncServiceServer extends UntypedServiceImplementation {
  contacts: handleUnaryCall<ContactSyncReq, ContactSyncResp>;
  mailboxes: handleUnaryCall<MailboxSyncReq, MailboxSyncResp>;
}

export interface SyncServiceClient extends Client {
  contacts(
    request: ContactSyncReq,
    callback: (error: ServiceError | null, response: ContactSyncResp) => void,
  ): ClientUnaryCall;
  contacts(
    request: ContactSyncReq,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ContactSyncResp) => void,
  ): ClientUnaryCall;
  contacts(
    request: ContactSyncReq,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ContactSyncResp) => void,
  ): ClientUnaryCall;
  mailboxes(
    request: MailboxSyncReq,
    callback: (error: ServiceError | null, response: MailboxSyncResp) => void,
  ): ClientUnaryCall;
  mailboxes(
    request: MailboxSyncReq,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: MailboxSyncResp) => void,
  ): ClientUnaryCall;
  mailboxes(
    request: MailboxSyncReq,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: MailboxSyncResp) => void,
  ): ClientUnaryCall;
}

export const SyncServiceClient = makeGenericClientConstructor(
  SyncServiceService,
  "hermes.sync.SyncService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): SyncServiceClient;
  service: typeof SyncServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
